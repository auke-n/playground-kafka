# ADR 0006: Make the Web UI Event Flow Manually Stepped by Default

## Status

Accepted

## Decision

The web UI publishes one lifecycle event per button click in manual mode. Automatic payment and fulfillment consumers are enabled only with the Compose `automatic` profile.
